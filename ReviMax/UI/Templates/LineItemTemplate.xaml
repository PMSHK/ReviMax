<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ReviMax;component/GostSymbolManager/UI/UserControl/Resources/RmRadioButtonNoBorderStyle.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <DataTemplate x:Key="LineItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Margin="0,0,0,0" Grid.Column="0" Text="{Binding Name}" />
            <!--<RadioButton Margin="0,0,0,0" Grid.Column="0" Style="{StaticResource RmRadio}" Content="{Binding Name}" Command="{Binding SelectLine}"/>-->
        </Grid>
    </DataTemplate>
    
</ResourceDictionary>