<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ReviMax;component/GostSymbolManager/UI/UserControl/Resources/RmIconButtonStyle.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <SolidColorBrush x:Key="TextDim" Color="#BDBDBD"/>
    <DataTemplate x:Key="CableSystemItemElement">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="{Binding Title}"/>
                <TextBlock Foreground="{StaticResource TextDim}" Text="{Binding SubTitle}"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Style="{StaticResource RmIconButton}"
                    Content="⋯"
                    ToolTip="Редактировать стиль"
                    Command="{Binding Edit}"/>
        </Grid>
    </DataTemplate>
</ResourceDictionary>