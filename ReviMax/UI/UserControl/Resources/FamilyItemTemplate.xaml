<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:fa="http://schemas.fontawesome.io/icons/"
                    xmlns:conveter="clr-namespace:ReviMax.UI.Converters"
                    
                    >
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ReviMax;component/UI/UserControl/Resources/RmRadioButtonNoBorderStyle.xaml"/>
        
    </ResourceDictionary.MergedDictionaries>
    <conveter:BoolToIconConverter x:Key="BoolToIconConverter"/>
    <conveter:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
    
    <DataTemplate x:Key="FamilyItemTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <fa:ImageAwesome Grid.Column="0"
         Width="14"
         Height="14"
         Foreground="{Binding IsLoaded, Converter={StaticResource BoolToBrushConverter}}"
         VerticalAlignment="Center"
         HorizontalAlignment="Center"
         Icon="{Binding IsLoaded, Converter={StaticResource BoolToIconConverter}}"/>
            <TextBlock Grid.Column="1" Margin="0,0,0,0" Text="{Binding Title}"/>
        </Grid>
    </DataTemplate>
    
</ResourceDictionary>